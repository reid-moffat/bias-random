name: CI
on:
  push:
    branches:
      - "**"

jobs:
  test:
    uses: ./.github/workflows/reusable-matrix.yml
    with:
      use-matrix: true
      run-command: pnpm run test
      run-command-description: Test
