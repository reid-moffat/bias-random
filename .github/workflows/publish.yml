name: Publish
on:
  push:
    branches:
      - "**"
      #- main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build:
    uses: ./.github/workflows/reusable-matrix.yml
    with:
      use-matrix: false
      action: changesets/action@v1
      run-command-description: Create Changeset PR
